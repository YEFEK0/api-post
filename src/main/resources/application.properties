server.port=${SERVER_PORT:8080}
spring.application.name=${NAME:api-post}
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5433/api-post-prueba}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:123456789}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
#Muestra las consultas en la consola 
# Formatea el SQL en la consola para que sea legible
spring.jpa.properties.hibernate.format_sql=true